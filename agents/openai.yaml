interface:
  display_name: "FPGA Project Reader"
  short_description: "Map FPGA projects, deep-read RTL paths, and annotate single files."
  default_prompt: "Use $fpga-project-reader to analyze this FPGA project, identify the main data links, deep-read selected paths, or make a single RTL/source file beginner-readable with first-principles comments."
